hi
{
  "name": "repo-updater",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "repo-updater": "./cli.js"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "commander": "^11.0.0"
  }
}
{
  "scripts": {
    "start": "node cli.js update",
    "build": "echo 'No build required' && exit 0"
  }
}
